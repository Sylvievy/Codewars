/*
make a given number negative

8kyu
*/

function makeNegative(num) {
    if (num>0){
      return num-(num+num)
    }
    return num
  }

/*
function makeNegative(num) {
  return num > 0 ? -num : num;
*/
